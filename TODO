
Reconstruire le container avec libGD :
docker-compose build --pull --no-cache phpapp

Démarrer avec le nouveau container
docker-compose stop phpapp && docker-compose rm -f phpapp && docker-compose up -d phpapp

